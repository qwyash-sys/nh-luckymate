# CLAUDE.md

NH농협은행 사내 공모전(AX Build-up Challenge) 제출용 미니앱 "NH행운메이트" 프로젝트입니다.

## 컨텍스트
이 폴더의 `NH행운메이트_프로젝트_인수인계.md`를 세션 시작 시 항상 먼저 읽고 작업을 시작할 것.
그 문서에 프로젝트 배경, 6개 화면 상세 스펙, 기술스택, 지금까지의 산출물, 남은 작업이 정리되어 있음.

## 프로젝트 성격
- 올원뱅크 인앱 미니앱으로 구현될 HTML5 기반 자체 개발 프로토타입
- 실제 배포가 아니라 **공모전 심사용 프로토타입/데모** — 완벽한 백엔드보다 화면 완성도와 아이디어 구현이 우선
- 최종 제출 마감: 2026년 10월

## 기술 원칙
- 프론트엔드: 순수 HTML5 / CSS3 / JavaScript (ES6+). 프레임워크(React 등) 쓰지 말고 단일 HTML 파일로 완결되게 작성
- 외부 CDN 로드가 막힌 환경일 수 있으니, 차트/그래프는 Chart.js 등 외부 라이브러리 대신 순수 JS/CSS로 직접 그릴 것 (div 높이 %, SVG 등)
- 저장은 LocalStorage 우선, Supabase는 실제 백엔드가 필요한 기능(로또 명당 데이터 등)에서만 사용
- 동행복권 데이터는 `https://www.dhlottery.co.kr/lt645/selectPstLt645Info.do?srchLtEpsd=all` 비공식 엔드포인트 사용 가능 (CORS 이슈 없음). 보고서/주석에는 "비공식"이라고 명시할 것 — 정식 Open API 아님

## 디자인 톤
- **현재 목업(`nh_luckymate_mockup.html` 등 3개)은 임시 초안**임. NH 그린(#00A651 계열) 기반 대략적인 목업이고, 최종 디자인 톤이 아님
- 올원뱅크 실제 앱 UI를 학습한 별도 디자인 결과물이 나오면, 그 결과를 기준으로 색상/타이포/컴포넌트 스타일을 전면 교체할 예정 — 그 전까지는 색상·톤에 과도하게 시간 쓰지 말고 기능 구현에 집중할 것
- 구조(.phone 클래스, 상태바+헤더+콘텐츠+하단 탭바)는 유지하되, 세부 스타일(색상, 폰트, 여백 등)은 디자인 결과물이 나오는 대로 갈아끼울 수 있게 CSS를 변수(CSS custom properties)로 분리해서 작성 — 예: `--primary-color`, `--primary-dark` 등을 :root에 정의해두면 나중에 값만 바꿔서 전체 톤을 쉽게 교체 가능

## 작업 방식
- 화면 하나 만들 때마다 실행 결과를 바로 확인시켜줄 것 (파일 경로 안내 또는 스크린샷)
- 큰 작업 전에 먼저 계획을 짧게 설명하고 진행
- 보고서용 표현(예: API 명칭)에서 과장하지 말 것 — "Open API" 대신 "비공식 API" 등 정확한 표현 사용

# NH행운메이트 — 진행 상황 (PROGRESS.md)

마지막 갱신: 2026-09-14 (동행복권 API 라이브 연동 + QR스캔/번호선택 UX 개선 + 세부 수정사항 대량 반영 + 로또 명당 API 발견)

---

## 1. 전체 요약

NH농협은행 사내 공모전(AX Build-up Challenge) 제출용 미니앱 "NH행운메이트" 프로토타입.
**메인 화면 6개 + 서브 화면 2개 + 네비게이션 허브(`index.html`), 총 8개 화면이 전부 완성**된 상태.
다국어(5개국어) i18n과 jsQR 카메라 스캔은 실제로 동작하는 수준까지 구현됨.
동행복권 API 라이브 연동(당첨조회·실수령액·3등챌린지), 카카오맵 웹 검색 링크(길찾기)까지 실제로 동작함.
반면 로또 명당 실데이터 백엔드, 카카오맵 API(SDK) 정식 연동, 방문 예약 실연동은 **아직 UI/링크 수준 또는 팝업 안내 수준**임.

## 2. 구현 완료된 기능

### 화면 목록
| # | 화면 | 파일 | 상태 |
|---|---|---|---|
| 1 | 당첨번호 조회 | `nh_luckymate_mockup.html` | 완료 (**동행복권 비공식 API 실시간 연동**, QR스캔/공선택 UI) |
| 1-1 | 번호 통계 분석 | `nh_luckymate_stats.html` | 완료 (**동행복권 실시간 데이터 연동**) |
| 1-2 | 실수령액 계산기 | `nh_luckymate_payout.html` | 완료 (**실시간 데이터 + 내 실제 결과 반영, 1~5등 전체**) |
| 2 | 내 근처 로또 명당 | `nh_luckymate_spots.html` | 완료 (정렬 3종 실동작, 데이터는 데모 샘플) |
| 3 | 당첨자 행동요령 가이드 | `nh_luckymate_guide.html` | 완료 (**카카오맵 웹링크 실동작**, 방문예약은 확인 팝업) |
| 4 | 낙첨복권 우대금리 | `nh_luckymate_bonus.html` | 완료 + **jsQR 실동작 (QR 인증주소 등록 방식으로 재설계)** |
| 5 | 3등 당첨 챌린지 | `nh_luckymate_challenge.html` | 완료 (**동행복권 실시간 데이터 기반 통계로 재설계**) |
| 6 | 언어 설정 | `nh_luckymate_language.html` | 완료 |
| - | 네비게이션 허브 | `index.html` | 완료 (8개 화면 카드로 연결) |

### 기능별 상세
- **번호 조회 및 자동 채점 (실시간 API 연동)**: 동행복권 비공식 엔드포인트(`selectPstLt645Info.do?srchLtEpsd=all`)에서 전체 회차 데이터를 fetch해 localStorage에 6시간 캐싱, 최신 회차를 기본 표시하고 최근 30개 회차를 드롭다운으로 선택 가능. 번호 볼 색상은 로또 표준 배색(1-10 노랑/11-20 파랑/21-30 빨강/31-40 회색/41-45 초록) 규칙으로 실제 번호에 맞춰 렌더링. 네트워크 실패 시 데모 데이터로 자동 폴백하고 안내 문구 표시. 최근 출현빈도 바 차트(1~45번 전체, 최근 50회차 기준)도 실제 데이터로 계산 (순수 JS/CSS DOM 방식, Chart.js 미사용)
- **내 번호 확인 UX (2026-09-13 개선)**: "QR코드로 확인" / "직접 번호 입력하기" 두 가지 방식 탭으로 분리.
  - QR코드로 확인: `getUserMedia`로 기본 카메라를 열어 jsQR로 실시간 스캔(자체 정의 포맷 `LOTTO|회차|번호6개` 인식) → 스캔 즉시 해당 회차·번호로 자동 전환되고 결과까지 자동 계산됨 ("자동으로 동행복권 연동"). 카메라 접근이 안 되는 환경을 위해 최신 회차 실제 당첨번호로 채우는 데모 스캔 버튼도 제공
  - 직접 번호 입력하기: 기존의 콤마 구분 텍스트 입력 방식을 폐기하고, 숫자를 한두 자리 입력하면 해당 접두사로 시작하는 후보 번호들이 공 모양 툴팁으로 가로로 나열되는 방식으로 변경 (예: "1" 입력 → 1,10,11,...,19 후보 표시 → 클릭하면 선택). 이미 선택한 번호·범위(1~45) 실시간 검증, 확인 버튼 클릭 시 부족한 개수 안내. 선택된 번호는 상단 당첨번호 조회와 동일한 색상 공 칩으로 표시되며 클릭하면 삭제 가능
  - jsQR 라이브러리는 `nh_luckymate_bonus.html`과 동일하게 이 파일에도 전체 인라인 삽입됨 (파일이 커진 이유 — 외부 CDN 미사용 원칙 유지)
- **번호 통계 분석**: 45개 번호 전체 출현빈도, 최다출현·미출현 TOP5 시각화
- **실수령액 계산기**: 등수별 탭 전환, 세금 공제 내역 표시, 예금 이자 연계 계산
- **로또 명당 찾기 (2026-09-13 개선)**: 거리순/당첨횟수순/당첨금액순 3개 탭이 실제로 다른 순서로 재정렬됨(데모 샘플 3곳 기준 winCount/amount 필드로 정렬). 각 판매점 "길찾기 ›"는 카카오맵 웹 검색(`map.kakao.com/link/search/`)으로 실제 연결됨
- **당첨자 행동요령 가이드 (2026-09-13 개선)**: 등수별(1등/2등/3등 이하) 4단계 지급 절차, 서대문 본점 안내, 당첨금 규모별 금융상품 연계. "카카오맵 길찾기"는 실제 카카오맵 웹 검색 링크로 동작(새 탭). "방문 예약하기"는 아직 실제 예약 시스템이 없어 "올원뱅크 방문하기 페이지로 이동합니다" 확인/취소 팝업만 표시(실연동 전 placeholder)
- **낙첨복권 우대금리 (2026-09-13 재설계)**: 실제 동행복권 QR은 당첨 여부를 담지 않고 인증 사이트 주소로만 연결된다는 점을 반영해, "QR 스캔 → 인증 주소 자체를 저장(URL이 곧 고유 키라 중복 등록 자동 차단)" 방식으로 전면 변경. 등록된 주소를 누르면 새 탭으로 열어 동행복권 사이트에서 직접 확인 가능. **등록은 30일 후 자동 만료·삭제**(조회 시점마다 만료분 필터링). 우대금리 3단계 안내가 실제 등록 건수에 따라 현재 해당 구간이 강조 표시됨(1장 이상/5장 이상/15장 이상). 데모 버튼은 실제 최신 회차 당첨번호를 담은 그럴듯한 인증 URL로 등록을 시연
- **3등 당첨 챌린지 (2026-09-13 재설계)**: 의미 없던 "참여자 수·참여율·챌린지 참여하기" 가짜 게이미피케이션 요소를 제거. 대신 동행복권 실시간 데이터(mockup.html과 캐시 공유)로 최근 12주 실제 3등 당첨자 수 차트, 실제 3등 평균 당첨금(약 140만원대 — 기존 "약 1,500만원"은 실제보다 10배 부풀려진 오류였음), 실제 평균 당첨자 수(회당 약 3,000명대 — 기존 "주 평균 90명"도 오류였음)로 교체. 히어로 영역은 다음 추첨일까지 D-day 카운트다운으로 대체. CTA는 당첨조회 화면으로 연결
- **실수령액 계산기 (2026-09-13 재설계)**: 1~5등 전체 탭 지원(기존엔 1~3등만). 당첨조회 화면에서 실제로 확인한 결과가 있으면(`localStorage: nh_last_check_result`) 그 회차·등수·실제 당첨금을 자동으로 반영해 "내 실제 결과"로 표시하고, 없으면 최신 회차 기준 "예시"로 표시. 4등(5만원)·5등(5천원)은 등수·당첨자 수와 무관한 고정 금액이라는 점과 세금 없이 전액 지급된다는 점을 별도 안내로 구분(1~3등만 세금 계산 내역 표시). 의미가 불분명했던 "공동 구매 인원" 스텝퍼는 제거
- **번호 통계 분석 (2026-09-13 실데이터 연동 + UX 개선)**: 3개 세그먼트 탭(출현빈도/미출현기간/구간별분포) 전부 실제로 다른 차트를 보여주도록 구현. "최근 50회" 배지는 실제 동작하는 드롭다운(30/50/100/전체)으로 교체되어 선택 시 헤더의 회차 범위·차트·TOP5가 전부 다시 계산됨. 모든 막대 차트에 마우스오버 툴팁 추가(번호/구간명 + 값 표시). "미출현 기간"은 전체 히스토리가 아니라 **선택된 범위 안에서** 마지막 출현 이후 경과 회차 수로 재정의(드롭다운과 일관되게). "구간별 분포"는 1-10/11-20/21-30/31-40/41-45 5구간으로 나눠 각 구간 출현 합계를 보여주며, 막대 색이 당첨조회 화면과 동일한 로또 표준 배색을 그대로 사용. 하단 TOP5 번호 원(circle)도 고정된 초록/빨강이 아니라 당첨조회와 동일한 번호별 표준 배색으로 변경, 5개가 좌측으로 쏠리던 배치도 균등 배치(space-between)로 수정. 범례 문구 "최근 15회 미출현"이 실제 로직(TOP5 기준)과 안 맞던 것도 "장기 미출현 TOP5"로 정정. 하단 CTA "내 번호와 비교 분석하기"는 실제로 비교 기능이 없는 payout.html로 연결되던 것을, 실제 번호 확인 기능이 있는 mockup.html(당첨조회, 직접 번호 입력하기)로 연결되도록 수정하고 문구도 "내 번호 입력하고 당첨 확인하기"로 정정
- **다국어 지원**: **8개 화면 전부에 `data-i18n` 속성 + 언어별 딕셔너리(ko/en/zh/vi/th/uz) 방식으로 실제 적용 완료**. 화면당 19~32개 텍스트 키가 번역되어 있고, 언어 전환 시 화면 전체 텍스트가 즉시 바뀜

### 기술적으로 해결한 이슈
- **외부 CDN 차단 환경**: Chart.js 같은 외부 라이브러리를 `<script src="cdnjs...">`로 로드하면 실패하는 환경이라, 모든 차트를 **순수 JS/CSS로 DOM 기반 바 차트 직접 구현**(div 높이 %) 방식으로 우회
- **jsQR 통합**: 외부 CDN 의존을 피하기 위해 jsQR 라이브러리 소스 전체를 `nh_luckymate_bonus.html`에 인라인으로 삽입 (파일이 10,560줄로 커진 이유)
- **디자인 톤 분리**: 최종 디자인이 아직 안 나온 상태라, 색상/타이포를 CSS custom properties(`--primary-color` 등)로 `:root`에 분리해 나중에 값만 바꿔 전체 톤 교체 가능하도록 구조화
- **구조 재사용**: `.phone` 클래스(상태바+헤더+콘텐츠+하단 탭바) 구조를 모든 화면에서 동일하게 유지해 화면 간 일관성 확보

## 3. 남은 작업 (미착수 — 실제 기능 연동 단위)

- **카카오맵 API(SDK) 정식 연동**: 현재는 `map.kakao.com/link/search/` 웹 검색 링크(새 탭 이동)로 대체 — API 키 없이 즉시 동작하는 실용적 임시 조치. 지도를 화면 안에 직접 표시(SDK 임베드)하려면 카카오 디벨로퍼스 앱 키 발급 필요
- **로또 명당 실데이터 백엔드**: 현재 하드코딩 데모 샘플 3곳만 사용 중(정렬 UI는 실동작). **당첨판매점 조회용 비공식 API를 실제로 찾아서 검증 완료(아래 참고)** — Geocoding 없이 위경도까지 바로 받아옴. Supabase 적재 스크립트와 spots.html 실연동 자체는 아직 미착수
- **낙첨복권 QR 등록 ↔ 영업점 우대금리 실연동**: QR 스캔해 인증 주소를 저장하는 것까지는 실동작하지만, "영업점 내방 시 이 등록 내역을 실제로 조회해서 우대금리 적용"하는 영업점 시스템 연동은 미착수
- **방문 예약 실연동**: `nh_luckymate_guide.html`의 "방문 예약하기"는 확인 팝업만 표시. 올원뱅크 실제 방문예약 화면 스크린샷을 받으면 그에 맞춰 흐름/디자인 반영 예정
- ~~번호 통계 분석(`nh_luckymate_stats.html`) 실데이터 연동~~ → **완료 (2026-09-13)**, 아래 참고

### 로또 명당 데이터 확보 방법 (2026-09-14, 실제 API 발견·검증 완료 — 적재 구현은 아직 미착수)

`dhlottery.co.kr/wnprchsplcsrch/home`(당첨 판매점 조회 페이지)를 직접 열어 네트워크 요청을 까본 결과, mockup.html이 쓰는 `selectPstLt645Info.do`와 같은 성격의 비공식 내부 API 두 개를 확인함.

**① 당첨 판매점 목록**
```
GET https://www.dhlottery.co.kr/wnprchsplcsrch/selectLtWnShp.do?srchWnShpRnk={all|1|2}&srchLtEpsd={회차번호}&srchShpLctn=
```
- 응답 필드: `shpNm`(상호명), `shpAddr`/`rdnm`(주소), `shpTelno`(전화번호), `region`(지역명), **`shpLat`/`shpLot`(위도/경도 — 이미 포함되어 있어 Geocoding 단계가 통째로 불필요)**, `ltShpId`(판매점 고유 ID — 회차마다 그룹핑하면 누적 집계 가능), `wnShpRnk`(해당 회차 1등/2등 여부), 취급 상품 여부(`l645LtNtslYn` 등)
- CORS 이슈 없음 — 브라우저에서 다른 origin(example.com)으로 직접 fetch 성공 확인
- **회차 단위로만 조회 가능**: `srchLtEpsd=all`은 "조회할 수 없는 회차입니다" 에러 반환. 여러 회차 누적하려면 회차별로 순회 호출해야 함
- `srchWnShpRnk=1`로 필터링해 1241회 조회 시 total=18 — mockup.html이 쓰는 API의 `rnk1WnNope: 18`과 정확히 일치 확인. `srchShpLctn`(지역 필터)은 값 체계 미확인(빈값=전체 지역은 확인됨)

**② 회차별 집계 카운트**
```
GET https://www.dhlottery.co.kr/wnprchsplcsrch/selectLtWnShpCnt.do?srchLtEpsd={회차번호}
```
전체/1등/2등 개수만 반환(예: 1241회 → all:108, rank1:18, rank2:90). ①의 `total`로 이미 충분히 대체 가능해서 꼭 필요하진 않음.

**적재 방법 (위경도가 이미 있어서 기존 계획보다 단순해짐)**:
1. **데이터 출처**: 위 ① API. 이용약관상 크롤링이 금지되어 있을 수 있어 **실서비스 전환 시 법무 검토 필요** — 프로토타입 단계에서는 지금의 데모 샘플로도 충분.
2. ~~주소 → 좌표 변환~~ → **불필요** (`shpLat`/`shpLot`가 응답에 이미 포함됨)
3. **누적 집계**: `ltShpId` 기준으로 회차별 결과를 그룹핑해 "1등 배출 횟수·2등 배출 횟수·최근 당첨 회차"를 계속 쌓아야 하므로 단순 파일이 아니라 DB(또는 DB 역할을 하는 로컬 JSON) 필요.

### 크롤러 구현 완료 (2026-09-14) — Supabase 대신 로컬 JSON DB로 우선 시연

Supabase 실제 구축(계정·프로젝트 생성) 전에, **이 Claude Code 세션의 Bash 환경에서 Node.js `fetch`로 실제 외부망(dhlottery.co.kr) 접근이 된다는 걸 확인**해서(⚠️ 같은 환경의 `curl`은 타임아웃 나서 안 됐지만 Node `fetch`는 성공 — 이 환경 특성으로 기억해둘 것), 별도 백엔드 없이 로컬에서 바로 크롤러를 만들고 실행했다.

- **스크립트**: `scripts/crawl_winning_shops.mjs` (의존성 없는 순수 Node.js, `node scripts/crawl_winning_shops.mjs`로 실행)
- **DB 파일**: `data/winning_shops.json` — `{ crawledRounds: [이미 수집한 회차 목록], shops: { [ltShpId]: {상호명,주소,전화,지역,위경도,rank1Count,rank2Count,lastWinRound} }, lastUpdated }`
- **옵션**:
  - `--count N` (기본 100): "최근 N회차"가 DB에 다 있도록 보장 — 이미 있는 회차는 건너뛰고 부족한 만큼만 새로 수집. 나중에 `--count 150`처럼 늘려서 재실행하면 새로 생긴 회차만 추가로 쌓임 (요청하신 "이미 db화된 회차는 제외하고 더 쌓기" 방식)
  - `--delay-min`/`--delay-max` (기본 1200~2500ms): 회차 조회 사이 무작위 지연 — 한꺼번에 콜을 몰아치지 않도록 함
  - 회차 하나 처리할 때마다 즉시 저장하므로 중간에 중단돼도 진행 상황이 남음
- **데이터 정제**: `ltShpId=51100000`("인터넷 복권판매사이트", 온라인 채널)는 실제 매장이 아니라서 수집 대상에서 자동 제외
- **매주 자동 축적**: 매주 같은 명령(`node scripts/crawl_winning_shops.mjs`)을 재실행하면 그사이 새로 열린 회차만 자동으로 채워짐. 스크립트 안에 macOS/Linux crontab 예시 주석으로 포함(`0 9 * * 0 ...` 매주 일요일 오전 9시). Claude Code 세션 자체의 정기 스케줄링(cron 에이전트 등)이 실제로 이 외부망 접근을 계속 보장하는지는 확인 안 됐으므로, 처음에는 사용자가 자기 컴퓨터의 crontab으로 직접 등록하는 걸 권장 — 필요하면 Claude Code 세션에서 대신 실행해주는 것도 가능(그때그때 요청)
- **검증**: `--count 3` → `--count 3`(재실행, 0건 추가 확인) → `--count 5`(2건만 추가 수집 확인) 순서로 스킵/증분 로직 실제 동작 확인 완료. 이후 `--count 100`으로 최근 100회차(1142회~1241회) 백필 완료 — 성공 100건/실패 0건, **누적 판매점 4,641곳**, `data/winning_shops.json` 약 1.6MB. 실제 데이터로 뽑아본 최다 당첨 판매점 TOP은 "뉴빅마트"(부산 기장군, 1등 10회·2등 15회) 등 — 완전히 실제 상호명/주소/전화/위경도 데이터임

### Supabase 연동 완료 (2026-09-14)

로컬 JSON을 넘어 실제 Supabase 프로젝트에 데이터를 올렸다. 배포(깃헙 페이지 프론트 + 깃헙 액션 크론 백엔드 + Supabase DB) 구조를 위한 준비.

- **스키마**: `supabase/schema.sql` — `winning_shops`(판매점별 누적: 상호명/주소/전화/지역/위경도/1등·2등 배출횟수/마지막 당첨회차), `crawled_rounds`(중복 수집 방지용 회차 목록). RLS 활성화, "읽기는 누구나(anon key), 쓰기는 service_role key만" 정책 적용
- **자격증명**: `.env`(프로젝트 루트, `.gitignore`에 포함되어 커밋 안 됨)에 `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` 저장
- **클라이언트**: `scripts/supabase_client.mjs` — `@supabase/supabase-js` 패키지 설치 없이 순수 `fetch`로 배치 upsert(500건씩 청크)하는 경량 헬퍼
- **1회성 이관**: `scripts/migrate_to_supabase.mjs` — 로컬 `data/winning_shops.json`에 있던 4,641곳을 Supabase로 업로드 완료
- **크롤러 연동**: `scripts/crawl_winning_shops.mjs`가 이제 회차별로 로컬 JSON 저장과 동시에 Supabase에도 upsert함(`.env` 있으면 자동 활성화, 없으면 로컬 JSON만 — 하위호환)
- **검증 완료**: anon key로 읽기 성공(실제 데이터, 1등순 정렬 확인) / anon key로 쓰기 시도 시 401로 차단(RLS 의도대로 동작) / 전체 4,641건 정확히 이관 확인

**아직 안 한 것**: `nh_luckymate_spots.html`이 Supabase(anon key)에서 실제로 읽어와 "내 근처" 정렬에 쓰도록 연결하는 작업(지금은 여전히 데모 샘플 3곳). GitHub Pages(프론트 배포) + GitHub Actions(크롤러 주간 스케줄) 설정도 미착수.

4. **백엔드 후보(장기)**: 실제 서비스로 갈 경우엔 PostGIS(위치 쿼리)와 관계형 집계를 함께 쓰기 좋은 **Supabase(Postgres 기반)**가 Firebase보다 적합 — Firestore는 지리공간 쿼리를 geohash로 직접 구현해야 해서 번거로움. 지금 프로토타입 단계는 위 로컬 JSON으로 충분해서 굳이 급하게 옮기지 않음.
5. **실행 위치**: 회차별 순회 호출과 집계는 브라우저가 아니라 위 Node 스크립트(로컬 실행 또는 향후 서버/스케줄러)에서 수행하고, 화면은 그 결과 파일/DB를 읽기만 하면 됨.

**완료됨 (2026-09-13)**: 동행복권 API 라이브 fetch — `nh_luckymate_mockup.html`, `nh_luckymate_challenge.html`, `nh_luckymate_payout.html`, **`nh_luckymate_stats.html`** 네 화면이 실제로 이 데이터를 fetch/공유해서 동작함 (아래 4번 항목 참고). stats.html은 최근 50회차 실제 범위 표시, 45개 번호 실제 출현빈도 차트, TOP5 최다출현/장기미출현(전체 히스토리 기준 미출현 기간 계산)까지 실데이터로 교체됨. 명당 정렬 UI, 카카오맵 웹링크, 방문예약 팝업, 실수령액 1~5등 확장, 3등챌린지 통계 재설계, 우대금리 QR 등록 방식 재설계도 모두 완료.

**미결정 사항**: 명당 데이터 백엔드 선택(Supabase vs Firebase) 및 실제 구현 착수 여부, 10월 마감 전 위 항목 중 어디까지 실제 구현할지 범위.

## 4. 겪은 이슈 / 노하우

- **외부 CDN 접근 차단**: 작업 환경에서 cdnjs.cloudflare.com 등 외부 CDN 접근이 막혀 있어 Chart.js 등을 `<script src>`로 불러오면 로드 실패함 → 순수 JS/CSS DOM 차트 또는 라이브러리 소스 인라인 삽입(jsQR 사례)으로 우회
- **동행복권 API 성격 오인 위험**: 비공식 엔드포인트를 "Open API"라고 표기하면 오해 소지 있음 → 보고서/주석에는 반드시 "비공식"이라고 명시하기로 함
- **인수인계 문서와 실제 진행 상태 괴리**: 세션이 여러 번 이어지면서 `NH행운메이트_프로젝트_인수인계.md`가 실제 구현 속도를 따라가지 못해, 이미 완료된 화면(명당/가이드/우대금리/챌린지/언어설정)이 "남은 작업(미착수)"로 잘못 기재되어 있던 것을 이번에 발견해 정정함. → 새 세션에서는 인수인계 문서만 믿지 말고 폴더 내 실제 파일을 재확인하는 습관 필요
- **복권 구매 고객 연령대 오류 정정**: 기획 초안에 "70%가 20~40대"로 잘못 기재되어 있던 것을 "약 75%가 40~60대"로 정정함 (전략 인사이트에 영향을 주는 수치라 중요)
- **스크린샷 캡처 시 `.phone` 요소만 locator로 잡아야 함**: 전체 페이지를 캡처하면 배경까지 나와 지저분해짐
- **동행복권 API 라이브 연동 (2026-09-13)**: `selectPstLt645Info.do?srchLtEpsd=all`은 전체 회차(현재 1241회까지) 데이터를 한 번에 반환(~750KB). 매번 전체를 새로 받지 않도록 localStorage에 6시간 TTL로 캐싱. 특정 회차만 필요하면 `srchLtEpsd=<회차번호>`로 단건 조회도 가능(확인 완료, 응답 훨씬 가벼움 — 추후 단건 조회 방식으로 최적화 여지 있음). CORS 이슈 없이 브라우저에서 바로 fetch 가능함을 실제 브라우저에서 재확인함
- **최상위 레벨 `localStorage` 접근은 항상 try/catch로 감쌀 것**: 검증 중 `const currentLang = localStorage.getItem(...)`처럼 감싸지 않은 최상위 코드가 storage 접근이 막힌 환경(사설 브라우징, 일부 인앱 웹뷰 등)에서 전체 스크립트 실행을 중단시키는 문제를 실제로 발견해 수정함. 올원뱅크 실제 웹뷰가 web storage를 제한할 가능성이 있으므로, 다른 화면 파일들도 동일 패턴(최상위 비try/catch localStorage 호출)이 있는지 추후 점검 필요
- **로컬 파일(`file://`) 미리보기 툴의 한계**: Claude 작업 환경의 브라우저 미리보기 도구는 로컬 HTML 파일을 `data:` URL 방식의 정적 스냅샷으로 렌더링해 `localStorage`/네트워크 fetch가 페이지 스크립트 내에서 정상 동작하지 않는 경우가 있었음 → 실제 동작 검증은 `python3 -m http.server`로 로컬 서버를 띄운 뒤 `http://localhost:PORT/...`로 접속해서 확인함 (파일 자체를 실제 브라우저에서 열거나 배포 후 접속하면 이 문제 없음)
- **`<button>` 요소에 `.cta` 같은 링크용 클래스를 그대로 씌우면 디자인이 깨짐 (2026-09-13)**: 브라우저 기본 button 스타일(appearance)이 `display:block`을 덮어써서 폭이 좁아지고 왼쪽 정렬되는 문제가 있었음 → 각 화면 CSS 최상단에 `button { all:unset; box-sizing:border-box; cursor:pointer; }` 리셋을 추가해 해결. 앞으로 `<button>`을 새로 추가하는 화면에서는 이 리셋이 있는지 먼저 확인할 것
- **캐시 스키마 버전 관리**: 동행복권 데이터 캐시(localStorage)에 필드를 추가할 때(`cnt` 배열 등) 캐시 키를 `nh_dh645_cache_v1` → `v2`로 올려서 구버전 캐시가 새 필드 없이 재사용되는 걸 방지함. 앞으로 캐시 데이터 구조를 바꿀 때는 항상 버전을 올릴 것 (모든 화면이 이 키를 공유하므로 한 곳만 바꾸면 안 됨 — mockup/challenge/payout 3개 파일 모두 동일 키 사용 중)

---
*이 문서는 `NH행운메이트_프로젝트_인수인계.md`(기획 배경·화면 스펙 중심)와 상호 보완적입니다. 인수인계 문서는 "왜/무엇을" 설명하고, 이 문서는 "지금 어디까지 됐고 무엇이 남았는지"를 추적하는 용도입니다.*

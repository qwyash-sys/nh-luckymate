# NH행운메이트 — 진행 상황 (PROGRESS.md)

마지막 갱신: 2026-09-23 (3등 챌린지 통합·실제 NH 상품 연계·명당 필터 강화)

---

## 1. 전체 요약

NH농협은행 사내 공모전(AX Build-up Challenge) 제출용 미니앱 "NH행운메이트" 프로토타입.
**메인 화면 5개(하단 탭) + 서브 화면 3개 + 네비게이션 허브(`index.html`), 총 9개 화면이 전부 완성**된 상태.
다국어(5개국어) i18n과 jsQR 카메라 스캔은 실제로 동작하는 수준까지 구현됨.
동행복권 API 라이브 연동(당첨조회·실수령액·3등챌린지), 카카오맵 웹 검색 링크(길찾기)까지 실제로 동작함.
로또 명당은 Supabase에 적재한 실데이터(최근 100회차·판매점 4,641곳)로 동작함.
반면 카카오맵 API(SDK) 정식 연동, 영업점 시스템 실연동, 방문 예약 실연동은 **아직 링크/팝업 안내 수준**임.

## 2. 구현 완료된 기능

### 화면 목록
하단 탭바 순서(2026-09-23 재편): **당첨조회 · 재미로보는통계 · 명당찾기 · 우대금리 · 당첨가이드**

| # | 화면 | 파일 | 상태 |
|---|---|---|---|
| 1 | 당첨번호 조회 | `nh_luckymate_mockup.html` | 완료 (**동행복권 비공식 API 실시간 연동**, QR스캔/공선택 UI) |
| 1-1 | 실수령액 계산기 | `nh_luckymate_payout.html` | 완료 (**실시간 데이터 + 내 실제 결과 반영, 1~5등 전체**) |
| 2 | 재미로 보는 통계 | `nh_luckymate_stats.html` | 완료 (**내 번호 상단 고정 + 과거 당첨이력, 3등 확률·NH상품 비교 포함 4개 카드섹션**) |
| 3 | 내 근처 로또 명당 | `nh_luckymate_spots.html` | 완료 (**Supabase 실데이터 4,641곳 연동**, 정렬 3종 실동작) |
| 4 | 낙첨복권 우대금리 | `nh_luckymate_bonus.html` | 완료 + **jsQR 실동작 (QR 인증주소 등록 방식으로 재설계)** |
| 5 | 당첨자 행동요령 가이드 | `nh_luckymate_guide.html` | 완료 (**등수별 절차·지급처 분기 실동작**, 카카오맵 웹링크, 방문예약은 확인 팝업) |
| 6 | 언어 설정 | `nh_luckymate_language.html` | 완료 |
| - | 네비게이션 허브 | `index.html` | 완료 (8개 화면 카드로 연결) |

### 기능별 상세
- **번호 조회 및 자동 채점 (실시간 API 연동)**: 동행복권 비공식 엔드포인트(`selectPstLt645Info.do?srchLtEpsd=all`)에서 전체 회차 데이터를 fetch해 localStorage에 6시간 캐싱, 최신 회차를 기본 표시하고 최근 30개 회차를 드롭다운으로 선택 가능. 번호 볼 색상은 로또 표준 배색(1-10 노랑/11-20 파랑/21-30 빨강/31-40 회색/41-45 초록) 규칙으로 실제 번호에 맞춰 렌더링. 네트워크 실패 시 데모 데이터로 자동 폴백하고 안내 문구 표시. 최근 출현빈도 바 차트(1~45번 전체, 최근 50회차 기준)도 실제 데이터로 계산 (순수 JS/CSS DOM 방식, Chart.js 미사용)
- **내 번호 확인 UX (2026-09-13 개선)**: "QR코드로 확인" / "직접 번호 입력하기" 두 가지 방식 탭으로 분리.
  - QR코드로 확인: `getUserMedia`로 기본 카메라를 열어 jsQR로 실시간 스캔(자체 정의 포맷 `LOTTO|회차|번호6개` 인식) → 스캔 즉시 해당 회차·번호로 자동 전환되고 결과까지 자동 계산됨 ("자동으로 동행복권 연동"). 카메라 접근이 안 되는 환경을 위해 최신 회차 실제 당첨번호로 채우는 데모 스캔 버튼도 제공
  - 직접 번호 입력하기: 기존의 콤마 구분 텍스트 입력 방식을 폐기하고, 숫자를 한두 자리 입력하면 해당 접두사로 시작하는 후보 번호들이 공 모양 툴팁으로 가로로 나열되는 방식으로 변경 (예: "1" 입력 → 1,10,11,...,19 후보 표시 → 클릭하면 선택). 이미 선택한 번호·범위(1~45) 실시간 검증, 확인 버튼 클릭 시 부족한 개수 안내. 선택된 번호는 상단 당첨번호 조회와 동일한 색상 공 칩으로 표시되며 클릭하면 삭제 가능
  - jsQR 라이브러리는 `nh_luckymate_bonus.html`과 동일하게 이 파일에도 전체 인라인 삽입됨 (파일이 커진 이유 — 외부 CDN 미사용 원칙 유지)
- **로또 명당 찾기**: 거리순/당첨횟수순/당첨금액순 3개 탭이 **Supabase 실데이터**로 재정렬됨(위치 기준 bounding-box 조회 → haversine 거리 계산). 당첨금액순은 등수별 평균 당첨금으로 추정한 값이라 "추정 누적"으로 명시. 각 판매점 "길찾기 ›"는 카카오맵 웹 검색(`map.kakao.com/link/search/`)으로 실제 연결됨
- **당첨자 행동요령 가이드 (2026-09-13 개선)**: 등수별(1등/2등/3등 이하) 4단계 지급 절차, 서대문 본점 안내, 당첨금 규모별 금융상품 연계. "카카오맵 길찾기"는 실제 카카오맵 웹 검색 링크로 동작(새 탭). "방문 예약하기"는 아직 실제 예약 시스템이 없어 "올원뱅크 방문하기 페이지로 이동합니다" 확인/취소 팝업만 표시(실연동 전 placeholder)
- **낙첨복권 우대금리 (2026-09-13 재설계)**: 실제 동행복권 QR은 당첨 여부를 담지 않고 인증 사이트 주소로만 연결된다는 점을 반영해, "QR 스캔 → 인증 주소 자체를 저장(URL이 곧 고유 키라 중복 등록 자동 차단)" 방식으로 전면 변경. 등록된 주소를 누르면 새 탭으로 열어 동행복권 사이트에서 직접 확인 가능. **등록은 30일 후 자동 만료·삭제**(조회 시점마다 만료분 필터링). 우대금리 3단계 안내가 실제 등록 건수에 따라 현재 해당 구간이 강조 표시됨(1장 이상/5장 이상/15장 이상). 데모 버튼은 실제 최신 회차 당첨번호를 담은 그럴듯한 인증 URL로 등록을 시연
- **3등 당첨 챌린지 (2026-09-13 재설계)**: 의미 없던 "참여자 수·참여율·챌린지 참여하기" 가짜 게이미피케이션 요소를 제거. 대신 동행복권 실시간 데이터(mockup.html과 캐시 공유)로 최근 12주 실제 3등 당첨자 수 차트, 실제 3등 평균 당첨금(약 140만원대 — 기존 "약 1,500만원"은 실제보다 10배 부풀려진 오류였음), 실제 평균 당첨자 수(회당 약 3,000명대 — 기존 "주 평균 90명"도 오류였음)로 교체. 히어로 영역은 다음 추첨일까지 D-day 카운트다운으로 대체. CTA는 당첨조회 화면으로 연결
- **실수령액 계산기 (2026-09-13 재설계)**: 1~5등 전체 탭 지원(기존엔 1~3등만). 당첨조회 화면에서 실제로 확인한 결과가 있으면(`localStorage: nh_last_check_result`) 그 회차·등수·실제 당첨금을 자동으로 반영해 "내 실제 결과"로 표시하고, 없으면 최신 회차 기준 "예시"로 표시. 4등(5만원)·5등(5천원)은 등수·당첨자 수와 무관한 고정 금액이라는 점과 세금 없이 전액 지급된다는 점을 별도 안내로 구분(1~3등만 세금 계산 내역 표시). 의미가 불분명했던 "공동 구매 인원" 스텝퍼는 제거
- **번호 통계 분석 (2026-09-13 실데이터 연동)**: 실시간 데이터로 45개 번호 출현빈도·미출현 기간을 계산하고 막대 차트에 마우스오버 툴팁 제공. 번호 원(circle)은 당첨조회와 동일한 로또 표준 배색 사용. → **2026-09-23 `재미로 보는 통계`로 전면 재설계됨(아래 '정보구조 전면 재편' 참고)**
- **다국어 지원**: **8개 화면 전부에 `data-i18n` 속성 + 언어별 딕셔너리(ko/en/zh/vi/th/uz) 방식으로 실제 적용 완료**. 화면당 19~32개 텍스트 키가 번역되어 있고, 언어 전환 시 화면 전체 텍스트가 즉시 바뀜

### 정보구조 전면 재편 (2026-09-23)
화면 간 중복과 의미 없는 진입점을 정리하고, "내 번호"를 축으로 흐름을 다시 묶음.

- **하단 탭 재정렬 + `재미로보는통계` 신설**: 탭 순서를 `당첨조회 · 재미로보는통계 · 명당찾기 · 우대금리 · 당첨가이드`로 변경. 기존 `번호 통계 분석`을 **`재미로 보는 통계`**로 개편해 2번 탭으로 승격하고, 독립 탭이던 **3등챌린지는 통계 화면 안의 카드섹션으로 흡수**(상세 화면 `challenge.html`은 하위 화면으로 유지, 뒤로가기도 통계 화면으로 연결). 8개 화면 전부 탭바를 동일 구조로 통일하고 6개 언어에 `tabStats` 키 추가
- **당첨조회 화면 중복 제거 및 동선 정리**: 통계 화면과 내용이 겹치던 "최근 50회차 번호별 출현 빈도" 차트 카드를 통째로 제거(해당 수치는 통계 화면에서 더 풍부하게 제공). 같은 곳(`bonus.html`)으로 가던 `QR로 낙첨 확인` / `우대금리 신청` 버튼 2개를 1개로 통합. **`당첨금 실수령액 계산하기` 버튼을 내 번호 결과 바로 아래로 이동**해 결과 → 실수령액 흐름이 이어지도록 배치하고, 그 아래에 통계 화면의 각 카드섹션으로 직접 점프하는 **2×2 바로가기 카드 4개**(`#secMyHistory` / `#secHot` / `#secCold` / `#secChallenge`) 신설
- **`재미로 보는 통계` 재설계 — 탭 → 세로 카드섹션**: 기존 3개 세그먼트 탭(출현빈도/미출현기간/구간별분포)을 폐기하고 아래로 늘어지는 카드섹션 구조로 전환. 각 섹션은 앵커 ID를 가져 당첨조회에서 바로 진입 가능
  - **상단 고정 "내 번호"**: 당첨조회에서 입력한 번호(`localStorage: nh_my_numbers`)를 최상단에 고정 표시하고, `수정` 버튼으로 그 자리에서 편집 가능(볼 클릭 삭제 + 숫자 입력 후보 선택, 당첨조회와 동일한 UX). 현재 회차 기준 등수·일치개수·당첨금도 함께 표시
  - **나의 번호, 과거엔 혹시 당첨?** (신규): 내 번호로 **전 회차(1,180여 회)를 전부 채점**해 당첨된 회차만 추출. `금액(등수)순` / `최근 순` 두 탭으로 정렬 전환, 4건씩 표시 후 `더보기`로 4건씩 확장. 각 행에 회차·날짜·등수 뱃지·당첨금과 함께 당첨번호 6개를 표시하되 **내가 맞힌 번호만 진하게, 못 맞힌 번호는 흐리게** 렌더링
  - **최근 많이 뽑힌 번호는?** (기존 출현빈도 개칭) / **최근 오랫동안 안 보인 번호는?** (기존 미출현기간 개칭): 차트·툴팁·TOP5는 유지하고 각 섹션이 **독립적인 범위 드롭다운**(30/50/100/전체)을 가짐. 요청에 따라 최다출현 번호·최다 출현 횟수·번호당 평균 출현, 최장 미출현 번호·미출현 회차·평균 미출현 **수치 지표 3종씩 추가**
  - **3등 챌린지**: 다음 추첨 D-day, 최근 12주 3등 당첨자 수 차트, 3등 평균 당첨금/확률/평균 당첨자 수. 재테크 상품 연계는 상세 화면으로 연결

### UX 개선 2차 (2026-09-23)
사용자 실사용 피드백 반영.

- **공통**: 헤더 우측 `⋮` → **🌐(언어)** 아이콘으로 변경. 언어 설정으로 가는 버튼인데 점 세 개는 의미가 안 드러났음
- **당첨조회**: ① 번호 6개 선택 시 좌측으로 쏠리던 배치를 균등 배치로 수정 ② **확인 버튼 제거** — 6개를 채우면 이미 자동 채점되고 있어 불필요했고, 다시 눌러도 아무 변화가 없어 버그처럼 보였음 ③ 하단 바로가기를 2×2 카드에서 **1×4 행 목록**으로, `보러가기 ›`는 `›`로 단순화
- **재미로보는통계**: ① **3등 챌린지를 카드섹션에 통합**하고 `challenge.html` 삭제(페이지를 나눌 이유가 없었음) ② **내 번호 카드를 상단 고정(sticky)** — 콘텐츠 영역만 스크롤하도록 바꿔 어떤 카드섹션을 보든 내가 고른 번호가 계속 보임 ③ **3등까지 몇 개 부족한지와 그 확률** 표시(맞춘 공은 유지하고 나머지만 다시 골랐을 때의 초기하분포: 4개 일치 **9.51%**, 3개 **1.02%**, 2개 **0.13%**) ④ 재테크 상품 나열을 **"3등에 쓸 기대 금액 vs NH 상품 이자" 비교**로 재구성(1,000원 × 41,404회 = 4,140만원)
- **명당찾기**: ① 당첨횟수순·당첨금액순에 **1등/2등/1·2등 합산** 세부 선택 ② 15개 고정 노출 → **10개씩 더보기** ③ **검색 반경 선택**(3/5/10/30/50/100km)
- **당첨가이드**: ① 방문 예약 안내를 "올원뱅크 영업점 예약하기 페이지로 이동합니다"로 수정 ② 금융상품을 **최신 회차 당첨금 기준 이자 계산**으로 교체

### UX 개선 3차 (2026-09-24)

- **공통**: 언어 버튼을 이모지 🌐(파랗고 촌스러움) → **흰색 선형 SVG 지구본**으로 교체
- **당첨조회 → 통계 링크**: 섹션으로 이동해도 **제목이 상단 고정 카드에 가려지던 문제** 수정. 원인은 브라우저가 `#해시`로 하는 자체 스크롤이 JS 스크롤을 덮어쓴 것이라, `scroll-margin-top`을 고정 카드 높이(CSS 변수 `--sticky-h`, 번호 유무에 따라 달라져 렌더링마다 동기화)만큼 주는 방식으로 변경
- **재미로보는통계**
  - 내 번호 6개가 좌측으로 쏠리던 배치 → 균등 배치
  - `최다출현 번호/최다 출현 횟수/번호당 평균 출현` 수치 3종을 제거하고, **차트 위에 평균선(`--- 평균 6.7회`)을 겹쳐 표시**. 아래에 **최소 출현 번호 TOP 5** 추가
  - 미출현 수치 3종도 동일하게 **평균선(`--- 평균 6.4회 미출현`)**으로 대체하고 **최근 출현 번호 TOP 5** 추가
  - **내 번호를 수정해도 3등 챌린지의 "몇 개만 더 맞췄다면" 안내가 갱신되지 않던 버그 수정**. 편집 완료 시 `renderNearMiss`를 다시 호출하지 않아 이전 번호 기준 문구가 남아 있었음 (사용자가 "요건이 적용 안 됐다"고 본 실제 원인)
- **당첨가이드**: 상품이 1개만 보이던 것을 **3종으로 확대**하고, 상품 한도(10억)가 아니라 **실제 당첨금 전액 기준**으로 이자를 계산하도록 변경. 1계좌 한도가 있는 상품은 "나눠 예치 기준"임을 함께 표기

### 세금 계산 정정 + 당첨가이드 재구성 (2026-09-24)

#### ⚠️ 기타소득세를 전액 20%로 계산하고 있었음
복권 당첨금 기타소득세는 **3억원까지 20%, 3억원 초과분 30%**(지방소득세는 소득세의 10%)인데,
실수령액 계산기가 전액에 20%를 곱하고 있었음. 3억을 크게 넘는 1등에서 세금이 크게 빗나감.

| | 이전 | 정정 후 |
|---|---|---|
| 세금 | 721,826,435원 | **1,049,739,653원** |
| 실수령 | 2,559,202,815원 | **2,231,289,597원** |

3.3억 차이. 구간이 갈리면 "3억원 이하분 20% / 3억원 초과분 30%"로 나눠 표시하고,
지방소득세 표기도 "당첨금의 2%"가 아니라 실제 기준인 "소득세의 10%"로 정정.
**당첨가이드의 예치 원금도 세전 당첨금이 아니라 세후 실수령액 기준**으로 수정.

#### 낙첨복권 삭제를 소프트 삭제로 변경
QR 주소가 복권 한 장의 고유 키인데 삭제 시 기록까지 지워서, **같은 복권을 다시 등록해
우대금리를 또 받을 수 있었음**. 사용 여부를 서버에 두지 않고 로컬에만 두는 구조라 흔적이 남아야 함.

- `active:false`로 바꾸는 소프트 삭제. 목록에서만 빠지고 기록은 남아 재등록이 차단됨
- 삭제 전 확인 팝업 (되돌릴 수 없다는 점 + 영업점에서 먼저 우대금리를 받으라는 안내)
- 이미 삭제한 복권과 보관함에 있는 복권을 구분해 다른 안내 표시
- 등록 유효기간 **30일 → 1년**

#### 금융상품 섹션을 "6개월 계획"과 연결 + 세금 표시 시각화
"잠깐만요(6개월간 큰 결정 미루기)" 카드와 상품 섹션이 따로 놀고 있어, 두 흐름을 하나로 이었다.

- 카드 제목을 등수별로: 1·2등 **"6개월, 어디에 둘까요?"** / 3등 이하 "당첨금, 어디에 둘까요?"
- 설명: "큰 결정을 미루는 동안 예금·적금 같은 안전한 곳에 맡겨두고, 이자를 받으며 천천히 계획을 세우세요"
- `"당첨금 3,281,029,250원 · 세금 떼고 2,231,289,598원 기준"`처럼 한 줄로 늘어놓아 읽기 어렵던 부분을
  **당첨금 → 세금 → 예치 가능 금액** 순으로 쌓아 보여주는 박스로 교체

#### 검증에서 추가로 잡은 것
- **화면 간 실수령액이 1원씩 달랐음**: 실수령액 계산기는 구간별 소득세를 각각 반올림해 더한 뒤 지방소득세를
  다시 반올림하는데, 당첨가이드는 소득세를 실수 그대로 1.1배 한 뒤 한 번에 반올림하고 있었음
  (계산기 2,231,289,597원 / 가이드 2,231,289,598원). 가이드를 계산기와 같은 순서로 통일
- **금융상품 카드 제목이 등수를 따라가지 않았음**: 등수별 제목을 데이터에 넣어놓고 렌더 코드에서 반영하는 줄이
  빠져 있어, 3등 이하에서도 "6개월, 어디에 둘까요?"가 그대로 보였음

#### 당첨가이드 재구성 (실제 수령 경험 반영)
절차만 나열하던 화면에 "지금 무엇을 하면 좋은지"를 더함. 사용자가 준 커뮤니티 글
두 편(로또 1등 행동수칙 / 실제 1등 수령 후기)을 참고.

- **"잠깐만요" 카드** (1·2등): 퇴사·집·차 같은 큰 결정은 6개월 미루고 평소처럼 지내라는 조언
- **등수별 현실 팁**
  - 1등(8개): 대중교통 이용(서대문역 4번 출구), 편한 복장, 월요일 대기가 긴 이유, 대기 중 건물 안에 머무르기, 복권 뒷면 서명, 이체 한도 미리 상향, 상담은 충분히 듣고 결정, 안전 걱정 불필요
  - 2등(3개) / 3등 이하(3개): 영업점·판매점 수령 기준
- 원문에 은행을 경계하라는 투로 적힌 대목은 사실관계는 유지하되 **"충분히 듣고 필요한 것만 고르셔도 된다"는 금융소비자 보호 관점**으로 다듬음

**참고 출처 메모**: 사용자가 준 링크 4개 중 클리앙·보배드림 글은 **같은 원본 이미지**(로또 1등 행동수칙)였고, 다음카페 글이 실제 1등 수령 후기로 가장 구체적이었음. **네이버 블로그는 이 환경에서 접근이 차단**되어 읽지 못함. 보배드림 댓글에서 나온 "당첨금은 수령 시 만든 계좌에 두기 / 자산은 묶어두고 생활비는 평소 수입으로"는 수령 후기의 본점 직원 조언과 겹쳐 근거가 확인돼 1등 팁에 추가함. 반면 "신협·새마을금고는 소문이 빠르다"는 특정 금융기관을 깎는 내용이라 넣지 않음

### 세금상담 AI챗봇 + 문구 점검 (2026-09-24)

#### 세금상담 AI챗봇 (전 화면 7개)
- 우측 하단 원형 버튼(말풍선 SVG). 마우스오버 시 "세금상담 AI챗봇" 툴팁
- 누르면 화면을 거의 꽉 채우는 팝업이 열리고, 우측 상단 닫기 버튼 하나만 있는 단순 구조. 배경 클릭·ESC로도 닫힘
- **챗봇 본체는 별도 구현 예정**이라 각 화면 스크립트의 `CHATBOT_URL` 상수만 채우면 iframe으로 바로 열림. 비어 있으면 안내 문구를 대신 표시
- 버튼을 폰 안쪽 하단에 고정하려면 페이지가 늘어나면 안 되므로, **콘텐츠 영역을 고정 높이(600px) + 내부 스크롤로 전 화면 통일**(이전엔 통계 화면만 적용)
- `button{all:unset}` 전역 리셋이 없는 화면이 있어, 챗봇 버튼에만 한정한 리셋을 따로 넣음

#### 문구·논리 점검에서 잡은 것
- **실수령액 계산기 계산 오류**: 하단 안내가 `"정기예금 3.2% 가입 시 연 158만원 이자"`로 **고정 문구**였음. 1등 실수령액 25.6억을 보고 있어도 158만원이라고 표시됐고 금리 3.2%도 실제와 달랐음. 실수령액으로 직접 계산하도록 바꾸고(NH올원e예금 3.55%) 금융소득 종합과세까지 반영 → 1등 기준 **연 90,851,700원(세전) / 75,402,851원(세후)**
- **우대금리 단계 문구가 로직과 불일치**: 3단계 조건이 코드상 `count >= 15`인데 화면에는 "최대 적용 한도"로 적혀 있어 조건이 아니라 상한처럼 읽혔음. 1단계에만 "월"이 빠져 기준도 뒤섞여 있었음 → **"1~4장 / 5~14장 / 15장 이상 등록"**으로 실제 조건에 맞춤
- **검증 불가한 최상급 표현**: 우대금리 헤더 "낙첨도 혜택이 되는 **유일한** 은행" → "낙첨 복권도 그냥 버리지 마세요"로 순화. 사내 공모전 제출물에 근거 없는 1위·유일 표현은 리스크

### UX 개선 4차 + 계산 오류 정정 (2026-09-24)

#### ⚠️ 3등 당첨 확률이 틀려 있었음 (1/41,404 → **1/35,724**)
화면에 `1/41,404`로 적혀 있었으나 검증 결과 성립할 수 없는 값이었음.

- 정확한 계산: 3등 = 당첨번호 6개 중 5개 + 나머지 1개는 **보너스가 아닌** 비당첨 38개 중 하나
  → `C(6,5) × 38 = 228`가지 ÷ `C(45,6) = 8,145,060` = **1/35,724**
- `41,404`로 역산하면 경우의 수가 `8,145,060 / 41,404 = 196.72`로 정수가 아니라 애초에 불가능한 값
- 교차검증: 1등 1/8,145,060, 2등 1/1,357,510 — 모두 일치
- 이에 따라 3등 기대 구매 금액도 **4,140만원 → 3,572만원**으로 정정

#### 금융소득 종합과세 반영 (당첨가이드)
세후 금액을 일반과세 15.4%로만 계산하던 것을, 연 이자가 2,000만원을 넘으면 종합과세로 계산하도록 변경.

- 2,000만원까지는 원천징수 14%, 초과분은 종합소득세 누진세율(6~45%) + 지방소득세 10%
- **1등 32.8억 예치 시 실효세율 20.0%** — 일반과세(15.4%)보다 확연히 높음
- 적용된 과세 방식을 배지(`금융소득 종합과세 20.0% 적용`)와 툴팁으로 구분 표시
- 다른 소득 없음·공제 미반영 단순 계산임을 화면에 명시
- 경계값(2,000만원 ±) 세후 역전이 없는 것과 전 구간 단조증가를 코드로 검증함

#### 그 외
- **통계**: TOP5 → **TOP7** 확대. 최소 출현 번호를 출현빈도 차트에 **빨간색**으로 표시해, 두 차트 모두 "자주/최근 = 초록, 드물게/오래 = 빨강"으로 색 의미를 통일. 최근 출현 번호에서 **직전 회차는 제외**(당연히 나온 번호라 의미 없음)하고 제목에도 명시
- **이자 표기**: 월만 보여주던 것을 **연간과 함께** 표시 (통계·가이드 공통)
- **적금 상품 교체**: 기존 NH직장인월복리적금은 월 한도 100만원이라 "3,572만원을 1년에 나눠 납입" 시나리오가 성립하지 않았음(화면만 봐서는 왜 100만원인지 알 수 없었음). **NH채움적금(월 한도 2,000만원, 연 3.23%, 우대조건 없음, 비대면 가입 가능)**으로 교체해 월 297만원 납입이 한도 안에서 성립. 한도를 넘는 경우에만 얼마가 필요한지 안내

#### 상품 구성 (2026-09-23 은행연합회 비교공시 기준, 세전·12개월 기본금리)
| 상품 | 금리 | 한도 |
|---|---|---|
| NH왈츠회전예금 II | 연 3.45% | **최고한도 공시 없음** — 거액 예치에 적합 |
| NH올원e예금 | 연 3.55% | 10만원~**10억원**(1계좌) |
| NH채움적금 | 연 3.23% | **월 2,000만원**(1인 총 2억) — 우대조건 없어 기본=최고 |

#### 금융상품 정보를 실제 공시값으로 교체 (중요)
화면에 있던 상품·금리가 실제와 달라 전수 확인 후 교체함.

- **`NH직장인든든적금 3.50%`는 실존하지 않는 상품**이었음(NH 저축상품 목록에 없음)
- **`NH올원e예금 3.20%`도 실제와 다름** — 실제 **연 3.55%**
- 교체 결과: **NH올원e예금 연 3.55%**(10만원~10억원, 비대면), **NH올원e적금 연 3.55%**(월 1천원~30만원, 12개월)
- 출처: 은행연합회 소비자포털 비교공시 + NH금융상품몰, **2026-09-23 기준, 세전·12개월**. 화면에도 기준일을 명시함
- 세후 금액은 **일반과세 15.4%**(이자소득세 14% + 지방소득세 1.4%) 적용. ⓘ 툴팁으로 "금융소득 종합과세 대상이 아닐 때 기준이며 종합과세 대상이면 달라질 수 있음"을 안내
- **펀드/ETF는 의도적으로 제외**함. 후보였던 `NH-Amundi 필승코리아`가 3개월 -26.63% / 1년 +143.46%로 기준일에 따라 숫자가 뒤집히고, 원금손실 가능 상품이라 "차라리 투자하세요" 맥락에 넣으면 오해 소지가 큼

### 배포 환경 전수 테스트에서 잡은 이슈 (2026-09-23)
GitHub Pages 실배포 URL에서 전 화면을 처음부터 다시 돌리며 확인한 것들.

- **동행복권 API 타임아웃이 너무 빡빡했음**: 실측 응답이 7.4초까지 걸리는데 코드 제한이 8초라, 간헐적으로 실데이터 대신 데모 데이터로 폴백되고 "네트워크 연결 실패" 문구가 떴음. **15초로 완화**해 5개 화면 전부 실데이터가 안정적으로 뜨도록 수정
- **통계에서 번호를 고쳐도 실수령액 계산기에 반영 안 됨**: 통계 화면의 내 번호 편집이 `nh_my_numbers`만 갱신하고 `nh_last_check_result`는 그대로라, 계산기에 이전 번호의 등수가 남아 있었음(통계는 3등인데 계산기는 1등). 편집 완료 시 최신 회차 기준 채점 결과를 함께 저장하도록 수정
- **가이드 화면의 등수 탭이 죽어 있었음**: `1등/2등/3등 이하` 탭에 **클릭 핸들러가 아예 없었고** 본문도 전부 1등 기준으로 하드코딩돼 있어, 눌러도 아무 변화가 없었음. 등수별로 지급 절차·지급처·카카오맵 목적지·연계 상품이 실제로 바뀌도록 데이터 기반으로 재구성 (1등=본점 전용 / 2등=전국 영업점 / 3등 이하=5만원 이하 판매점 수령 가능). 당첨금 지급 기한(1년) 안내도 추가

### 기술적으로 해결한 이슈
- **외부 CDN 차단 환경**: Chart.js 같은 외부 라이브러리를 `<script src="cdnjs...">`로 로드하면 실패하는 환경이라, 모든 차트를 **순수 JS/CSS로 DOM 기반 바 차트 직접 구현**(div 높이 %) 방식으로 우회
- **jsQR 통합**: 외부 CDN 의존을 피하기 위해 jsQR 라이브러리 소스 전체를 `nh_luckymate_bonus.html`에 인라인으로 삽입 (파일이 10,560줄로 커진 이유)
- **디자인 톤 분리**: 최종 디자인이 아직 안 나온 상태라, 색상/타이포를 CSS custom properties(`--primary-color` 등)로 `:root`에 분리해 나중에 값만 바꿔 전체 톤 교체 가능하도록 구조화
- **구조 재사용**: `.phone` 클래스(상태바+헤더+콘텐츠+하단 탭바) 구조를 모든 화면에서 동일하게 유지해 화면 간 일관성 확보
- **`<button>` 너비 축소 버그 (2026-09-23)**: `button { all:unset }` 리셋을 적용해도 `<button>`은 `display:block`에서조차 내용 크기로 shrink-to-fit되는 브라우저 고유 동작이 있어, `.cta`를 붙여도 전체 너비로 늘어나지 않고 좌측에 작게 표시됨. `.cta`에 **`width:100%`를 명시**해 해결(`<a class="cta">`에는 영향 없음). `<button>`에 `.cta`를 새로 붙일 때 재발 주의

### 재검증에서 잡은 것 (2026-09-24)
- **비한국어 문구 중복**: `flowGross` 템플릿에 "당첨금"에 해당하는 단어가 있는데 `{rank}` 값에도 같은 단어가 들어 있어
  영어 `"Round 1242 1st Prize prize"`, 베트남어 `"Giải Giải Nhất"`, 태국어 `"รางวัลรางวัลที่ 1"`로 겹쳤음 → 템플릿 정리
- **단위 표기 오류**: 만/억 단위를 전 언어에 쓰고 있었는데, 영어는 `"2,0000K"`로 깨지고
  **우즈베크어는 `ming`(1,000)이라 10배 틀린 값**이었음 → 한국어·중국어·베트남어만 만/억을 쓰고 나머지는 Intl 축약 표기로 대체
- 화면에서 더 이상 쓰지 않는 `prizeBaseTpl` 키 제거

## 3. 남은 작업 (미착수 — 실제 기능 연동 단위)

- **카카오맵 API(SDK) 정식 연동**: 현재는 `map.kakao.com/link/search/` 웹 검색 링크(새 탭 이동)로 대체 — API 키 없이 즉시 동작하는 실용적 임시 조치. 지도를 화면 안에 직접 표시(SDK 임베드)하려면 카카오 디벨로퍼스 앱 키 발급 필요
- ~~**로또 명당 실데이터 백엔드**~~ → **완료**. 비공식 당첨판매점 API로 최근 100회차를 크롤링해 Supabase에 적재(판매점 4,641곳)하고 `spots.html`이 직접 조회하도록 연결됨. 남은 것은 **GitHub Actions Secrets 등록**(주간 자동 수집 활성화)뿐
- **낙첨복권 QR 등록 ↔ 영업점 우대금리 실연동**: QR 스캔해 인증 주소를 저장하는 것까지는 실동작하지만, "영업점 내방 시 이 등록 내역을 실제로 조회해서 우대금리 적용"하는 영업점 시스템 연동은 미착수
- **방문 예약 실연동**: `nh_luckymate_guide.html`의 "방문 예약하기"는 확인 팝업만 표시. 올원뱅크 실제 방문예약 화면 스크린샷을 받으면 그에 맞춰 흐름/디자인 반영 예정
- ~~번호 통계 분석(`nh_luckymate_stats.html`) 실데이터 연동~~ → **완료 (2026-09-13)**, 아래 참고

### 로또 명당 데이터 확보 방법 (2026-09-14, 실제 API 발견·검증 완료 — 적재 구현은 아직 미착수)

`dhlottery.co.kr/wnprchsplcsrch/home`(당첨 판매점 조회 페이지)를 직접 열어 네트워크 요청을 까본 결과, mockup.html이 쓰는 `selectPstLt645Info.do`와 같은 성격의 비공식 내부 API 두 개를 확인함.

**① 당첨 판매점 목록**
```
GET https://www.dhlottery.co.kr/wnprchsplcsrch/selectLtWnShp.do?srchWnShpRnk={all|1|2}&srchLtEpsd={회차번호}&srchShpLctn=
```
- 응답 필드: `shpNm`(상호명), `shpAddr`/`rdnm`(주소), `shpTelno`(전화번호), `region`(지역명), **`shpLat`/`shpLot`(위도/경도 — 이미 포함되어 있어 Geocoding 단계가 통째로 불필요)**, `ltShpId`(판매점 고유 ID — 회차마다 그룹핑하면 누적 집계 가능), `wnShpRnk`(해당 회차 1등/2등 여부), 취급 상품 여부(`l645LtNtslYn` 등)
- CORS 이슈 없음 — 브라우저에서 다른 origin(example.com)으로 직접 fetch 성공 확인
- **회차 단위로만 조회 가능**: `srchLtEpsd=all`은 "조회할 수 없는 회차입니다" 에러 반환. 여러 회차 누적하려면 회차별로 순회 호출해야 함
- `srchWnShpRnk=1`로 필터링해 1241회 조회 시 total=18 — mockup.html이 쓰는 API의 `rnk1WnNope: 18`과 정확히 일치 확인. `srchShpLctn`(지역 필터)은 값 체계 미확인(빈값=전체 지역은 확인됨)

**② 회차별 집계 카운트**
```
GET https://www.dhlottery.co.kr/wnprchsplcsrch/selectLtWnShpCnt.do?srchLtEpsd={회차번호}
```
전체/1등/2등 개수만 반환(예: 1241회 → all:108, rank1:18, rank2:90). ①의 `total`로 이미 충분히 대체 가능해서 꼭 필요하진 않음.

**적재 방법 (위경도가 이미 있어서 기존 계획보다 단순해짐)**:
1. **데이터 출처**: 위 ① API. 이용약관상 크롤링이 금지되어 있을 수 있어 **실서비스 전환 시 법무 검토 필요** — 프로토타입 단계에서는 지금의 데모 샘플로도 충분.
2. ~~주소 → 좌표 변환~~ → **불필요** (`shpLat`/`shpLot`가 응답에 이미 포함됨)
3. **누적 집계**: `ltShpId` 기준으로 회차별 결과를 그룹핑해 "1등 배출 횟수·2등 배출 횟수·최근 당첨 회차"를 계속 쌓아야 하므로 단순 파일이 아니라 DB(또는 DB 역할을 하는 로컬 JSON) 필요.

### 크롤러 구현 완료 (2026-09-14) — Supabase 대신 로컬 JSON DB로 우선 시연

Supabase 실제 구축(계정·프로젝트 생성) 전에, **이 Claude Code 세션의 Bash 환경에서 Node.js `fetch`로 실제 외부망(dhlottery.co.kr) 접근이 된다는 걸 확인**해서(⚠️ 같은 환경의 `curl`은 타임아웃 나서 안 됐지만 Node `fetch`는 성공 — 이 환경 특성으로 기억해둘 것), 별도 백엔드 없이 로컬에서 바로 크롤러를 만들고 실행했다.

- **스크립트**: `scripts/crawl_winning_shops.mjs` (의존성 없는 순수 Node.js, `node scripts/crawl_winning_shops.mjs`로 실행)
- **DB 파일**: `data/winning_shops.json` — `{ crawledRounds: [이미 수집한 회차 목록], shops: { [ltShpId]: {상호명,주소,전화,지역,위경도,rank1Count,rank2Count,lastWinRound} }, lastUpdated }`
- **옵션**:
  - `--count N` (기본 100): "최근 N회차"가 DB에 다 있도록 보장 — 이미 있는 회차는 건너뛰고 부족한 만큼만 새로 수집. 나중에 `--count 150`처럼 늘려서 재실행하면 새로 생긴 회차만 추가로 쌓임 (요청하신 "이미 db화된 회차는 제외하고 더 쌓기" 방식)
  - `--delay-min`/`--delay-max` (기본 1200~2500ms): 회차 조회 사이 무작위 지연 — 한꺼번에 콜을 몰아치지 않도록 함
  - 회차 하나 처리할 때마다 즉시 저장하므로 중간에 중단돼도 진행 상황이 남음
- **데이터 정제**: `ltShpId=51100000`("인터넷 복권판매사이트", 온라인 채널)는 실제 매장이 아니라서 수집 대상에서 자동 제외
- **매주 자동 축적**: 매주 같은 명령(`node scripts/crawl_winning_shops.mjs`)을 재실행하면 그사이 새로 열린 회차만 자동으로 채워짐. 스크립트 안에 macOS/Linux crontab 예시 주석으로 포함(`0 9 * * 0 ...` 매주 일요일 오전 9시). Claude Code 세션 자체의 정기 스케줄링(cron 에이전트 등)이 실제로 이 외부망 접근을 계속 보장하는지는 확인 안 됐으므로, 처음에는 사용자가 자기 컴퓨터의 crontab으로 직접 등록하는 걸 권장 — 필요하면 Claude Code 세션에서 대신 실행해주는 것도 가능(그때그때 요청)
- **검증**: `--count 3` → `--count 3`(재실행, 0건 추가 확인) → `--count 5`(2건만 추가 수집 확인) 순서로 스킵/증분 로직 실제 동작 확인 완료. 이후 `--count 100`으로 최근 100회차(1142회~1241회) 백필 완료 — 성공 100건/실패 0건, **누적 판매점 4,641곳**, `data/winning_shops.json` 약 1.6MB. 실제 데이터로 뽑아본 최다 당첨 판매점 TOP은 "뉴빅마트"(부산 기장군, 1등 10회·2등 15회) 등 — 완전히 실제 상호명/주소/전화/위경도 데이터임

### Supabase 일시중지 → 복구 완료 (2026-09-23)
무료 플랜 프로젝트가 **미사용으로 자동 일시중지(pause)** 되어 `nslookup` 결과가 NXDOMAIN이었고,
`nh_luckymate_spots.html`이 빈 목록만 표시했음. 사용자가 대시보드에서 Restore 후 정상 복구 확인.

- **복구 확인**: DNS 재생성 → HTTP 521(기동 중) → 404 → **200**. 약 3분 소요
- **데이터 보존**: `winning_shops` **4,641건**, `crawled_rounds` **100건** 그대로 유지됨. 배포 환경에서 300곳 조회·3종 정렬 전부 정상 동작 재확인
- **⚠️ 재발 방지 (미조치)**: GitHub Actions Secrets(`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`)가 아직 미등록이라 **주간 크롤러가 한 번도 실행된 적 없음**. 이 때문에 Supabase가 계속 미사용 상태가 되어 이번 pause가 발생함. Secrets를 등록하면 매주 월요일 크롤러가 접근하므로 pause가 재발하지 않고, 신규 회차도 자동 누적됨
- **현재 데이터 공백**: 수집된 최신 회차는 **1241**인데 실제 최신은 **1242**(9/19 추첨). Secrets 등록 후 워크플로를 수동 실행(`workflow_dispatch`)하면 1242회차가 채워짐

### Supabase 연동 완료 (2026-09-14)

로컬 JSON을 넘어 실제 Supabase 프로젝트에 데이터를 올렸다. 배포(깃헙 페이지 프론트 + 깃헙 액션 크론 백엔드 + Supabase DB) 구조를 위한 준비.

- **스키마**: `supabase/schema.sql` — `winning_shops`(판매점별 누적: 상호명/주소/전화/지역/위경도/1등·2등 배출횟수/마지막 당첨회차), `crawled_rounds`(중복 수집 방지용 회차 목록). RLS 활성화, "읽기는 누구나(anon key), 쓰기는 service_role key만" 정책 적용
- **자격증명**: `.env`(프로젝트 루트, `.gitignore`에 포함되어 커밋 안 됨)에 `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` 저장
- **클라이언트**: `scripts/supabase_client.mjs` — `@supabase/supabase-js` 패키지 설치 없이 순수 `fetch`로 배치 upsert(500건씩 청크)하는 경량 헬퍼
- **1회성 이관**: `scripts/migrate_to_supabase.mjs` — 로컬 `data/winning_shops.json`에 있던 4,641곳을 Supabase로 업로드 완료
- **크롤러 연동**: `scripts/crawl_winning_shops.mjs`가 이제 회차별로 로컬 JSON 저장과 동시에 Supabase에도 upsert함(`.env` 있으면 자동 활성화, 없으면 로컬 JSON만 — 하위호환)
- **검증 완료**: anon key로 읽기 성공(실제 데이터, 1등순 정렬 확인) / anon key로 쓰기 시도 시 401로 차단(RLS 의도대로 동작) / 전체 4,641건 정확히 이관 확인

**이후 완료됨 (2026-09-23)**: `nh_luckymate_spots.html`의 Supabase 실연동, GitHub 저장소 생성·푸시, GitHub Pages 배포까지 전부 완료.
배포 주소: **https://qwyash-sys.github.io/nh-luckymate/**

**2026-09-23 최종 완료**: GitHub Actions Secrets 등록 후 워크플로를 수동 실행해 **백엔드까지 실제 동작 확인**.

- 결과: 수집 회차 100 → **101개**(1242회차 추가), 판매점 4,641 → **4,667곳**(신규 26곳). 1242회 배출 판매점 105곳 정상 수집
- **증분 수집 검증됨**: 100회차를 다시 긁지 않고 이미 있는 회차는 건너뛴 뒤 새로 열린 1회차만 수집함
- 이후 **매주 월요일 오전 10시(KST) 자동 실행**되며, 이 접근 덕분에 Supabase가 미사용으로 pause되는 일도 방지됨

**Secrets 등록 시 주의(실제로 겪음)**: 219자짜리 service_role key를 터미널 화면에서 마우스로 긁어 복사하면
줄이 접히는 위치에 개행이 섞여 들어가 `Headers.append: invalid header value`로 모든 쓰기가 실패한다.
`grep ... | tr -d '[:space:]' | pbcopy`처럼 클립보드로 직접 넣을 것. 현재는 클라이언트에서도 공백·개행을 제거한다.

4. **백엔드 후보(장기)**: 실제 서비스로 갈 경우엔 PostGIS(위치 쿼리)와 관계형 집계를 함께 쓰기 좋은 **Supabase(Postgres 기반)**가 Firebase보다 적합 — Firestore는 지리공간 쿼리를 geohash로 직접 구현해야 해서 번거로움. 지금 프로토타입 단계는 위 로컬 JSON으로 충분해서 굳이 급하게 옮기지 않음.
5. **실행 위치**: 회차별 순회 호출과 집계는 브라우저가 아니라 위 Node 스크립트(로컬 실행 또는 향후 서버/스케줄러)에서 수행하고, 화면은 그 결과 파일/DB를 읽기만 하면 됨.

**완료됨 (2026-09-13)**: 동행복권 API 라이브 fetch — `nh_luckymate_mockup.html`, `nh_luckymate_challenge.html`, `nh_luckymate_payout.html`, **`nh_luckymate_stats.html`** 네 화면이 실제로 이 데이터를 fetch/공유해서 동작함 (아래 4번 항목 참고). stats.html은 최근 50회차 실제 범위 표시, 45개 번호 실제 출현빈도 차트, TOP5 최다출현/장기미출현(전체 히스토리 기준 미출현 기간 계산)까지 실데이터로 교체됨. 명당 정렬 UI, 카카오맵 웹링크, 방문예약 팝업, 실수령액 1~5등 확장, 3등챌린지 통계 재설계, 우대금리 QR 등록 방식 재설계도 모두 완료.

**미결정 사항**: 명당 데이터 백엔드 선택(Supabase vs Firebase) 및 실제 구현 착수 여부, 10월 마감 전 위 항목 중 어디까지 실제 구현할지 범위.

## 4. 겪은 이슈 / 노하우

- **외부 CDN 접근 차단**: 작업 환경에서 cdnjs.cloudflare.com 등 외부 CDN 접근이 막혀 있어 Chart.js 등을 `<script src>`로 불러오면 로드 실패함 → 순수 JS/CSS DOM 차트 또는 라이브러리 소스 인라인 삽입(jsQR 사례)으로 우회
- **동행복권 API 성격 오인 위험**: 비공식 엔드포인트를 "Open API"라고 표기하면 오해 소지 있음 → 보고서/주석에는 반드시 "비공식"이라고 명시하기로 함
- **인수인계 문서와 실제 진행 상태 괴리**: 세션이 여러 번 이어지면서 `NH행운메이트_프로젝트_인수인계.md`가 실제 구현 속도를 따라가지 못해, 이미 완료된 화면(명당/가이드/우대금리/챌린지/언어설정)이 "남은 작업(미착수)"로 잘못 기재되어 있던 것을 이번에 발견해 정정함. → 새 세션에서는 인수인계 문서만 믿지 말고 폴더 내 실제 파일을 재확인하는 습관 필요
- **복권 구매 고객 연령대 오류 정정**: 기획 초안에 "70%가 20~40대"로 잘못 기재되어 있던 것을 "약 75%가 40~60대"로 정정함 (전략 인사이트에 영향을 주는 수치라 중요)
- **스크린샷 캡처 시 `.phone` 요소만 locator로 잡아야 함**: 전체 페이지를 캡처하면 배경까지 나와 지저분해짐
- **동행복권 API 라이브 연동 (2026-09-13)**: `selectPstLt645Info.do?srchLtEpsd=all`은 전체 회차(현재 1241회까지) 데이터를 한 번에 반환(~750KB). 매번 전체를 새로 받지 않도록 localStorage에 6시간 TTL로 캐싱. 특정 회차만 필요하면 `srchLtEpsd=<회차번호>`로 단건 조회도 가능(확인 완료, 응답 훨씬 가벼움 — 추후 단건 조회 방식으로 최적화 여지 있음). CORS 이슈 없이 브라우저에서 바로 fetch 가능함을 실제 브라우저에서 재확인함
- **최상위 레벨 `localStorage` 접근은 항상 try/catch로 감쌀 것**: 검증 중 `const currentLang = localStorage.getItem(...)`처럼 감싸지 않은 최상위 코드가 storage 접근이 막힌 환경(사설 브라우징, 일부 인앱 웹뷰 등)에서 전체 스크립트 실행을 중단시키는 문제를 실제로 발견해 수정함. 올원뱅크 실제 웹뷰가 web storage를 제한할 가능성이 있으므로, 다른 화면 파일들도 동일 패턴(최상위 비try/catch localStorage 호출)이 있는지 추후 점검 필요
- **로컬 파일(`file://`) 미리보기 툴의 한계**: Claude 작업 환경의 브라우저 미리보기 도구는 로컬 HTML 파일을 `data:` URL 방식의 정적 스냅샷으로 렌더링해 `localStorage`/네트워크 fetch가 페이지 스크립트 내에서 정상 동작하지 않는 경우가 있었음 → 실제 동작 검증은 `python3 -m http.server`로 로컬 서버를 띄운 뒤 `http://localhost:PORT/...`로 접속해서 확인함 (파일 자체를 실제 브라우저에서 열거나 배포 후 접속하면 이 문제 없음)
- **`<button>` 요소에 `.cta` 같은 링크용 클래스를 그대로 씌우면 디자인이 깨짐 (2026-09-13)**: 브라우저 기본 button 스타일(appearance)이 `display:block`을 덮어써서 폭이 좁아지고 왼쪽 정렬되는 문제가 있었음 → 각 화면 CSS 최상단에 `button { all:unset; box-sizing:border-box; cursor:pointer; }` 리셋을 추가해 해결. 앞으로 `<button>`을 새로 추가하는 화면에서는 이 리셋이 있는지 먼저 확인할 것
- **캐시 스키마 버전 관리**: 동행복권 데이터 캐시(localStorage)에 필드를 추가할 때(`cnt` 배열 등) 캐시 키를 `nh_dh645_cache_v1` → `v2`로 올려서 구버전 캐시가 새 필드 없이 재사용되는 걸 방지함. 앞으로 캐시 데이터 구조를 바꿀 때는 항상 버전을 올릴 것 (모든 화면이 이 키를 공유하므로 한 곳만 바꾸면 안 됨 — mockup/challenge/payout 3개 파일 모두 동일 키 사용 중)

---
*이 문서는 `NH행운메이트_프로젝트_인수인계.md`(기획 배경·화면 스펙 중심)와 상호 보완적입니다. 인수인계 문서는 "왜/무엇을" 설명하고, 이 문서는 "지금 어디까지 됐고 무엇이 남았는지"를 추적하는 용도입니다.*
